type Profile = {
    id: number, 
    username: string,
    avatar: string,
    profileColor: string,
    registrationDate: string,
    status: string,
    role: string,
    isSubscribed: boolean,
    canBan: boolean
};

export default Profile;